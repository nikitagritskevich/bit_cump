package web.iterface;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public interface InterfaceLogger {

    Logger interfaceLog = LogManager.getLogger(Footer.class);
}
